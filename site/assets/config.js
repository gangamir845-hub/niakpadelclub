// آدرس پایه‌ی API — تنها جایی که برای انتقال سایت به هاست خودت باید عوضش کنی.
//
// مقدار __PORT_8000__ فقط در محیط Perplexity معنا دارد: هنگام انتشار به آدرس
// بک‌اند همین پروژه (پورت 8000) بازنویسی می‌شود.
//
// برای هاست خودت (روش توصیه‌شده: Nginx که /api/ را به بک‌اند پروکسی می‌کند)
// این مقدار را خالی بگذار:
//
//   window.NIAK_API_BASE = '';
//
// و اگر بک‌اند روی دامنه/پورت جدا است، آدرس کاملش را بگذار و دامنه‌ی سایت را هم
// به NIAK_ALLOWED_ORIGINS در .env اضافه کن:
//
//   window.NIAK_API_BASE = 'https://api.niakpadel.ir';
window.NIAK_API_BASE = '__PORT_8000__';
